[project]
name = "galgame-prompt-templates"
version = "0.1.0"
description = "LLM prompt templates for the galgame translater pipeline"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
